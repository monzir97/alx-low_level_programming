#include "main.h"
/**
 * mul- print multiplication for two numbers
 * @a: the first number.
 * @b: the second number
 * Return: it returns the multiplication for the two
 */
int mul(int a, int b)
{
return (a * b);
}
