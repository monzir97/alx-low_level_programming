#include "main.h"
/**
 * _isupper- function determine whether character is upper or not
 * @c:the character to be chekced
 * Return: it return 1 for upper 0 otherwise
 */
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
