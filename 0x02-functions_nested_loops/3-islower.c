#include "main.h"
/**
*_islower- return zero the lower
*@c: the letter
* Return: zero for the upper one the lower
 */
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}


}

